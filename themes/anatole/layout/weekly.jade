extends partial/layout

block site_title
    !=  "周报 · " + config.title

block description
    - var desc = '周报 - ' + config.title;
    meta(name="description", content=desc)

block content
    .autopagerize_page_element: .content
      .archive.animated.fadeInDown
        ul.list-with-title
          include mixins
          +weeklyList()
